# memo 74 — e108 组合信号深度调研（预登记 + 第①步冗余度解剖已落地）

日期：2026-09-25 | 触发：用户假设「几个弱信号数据种类通过组合可能产生强信号；
弱×弱、弱×强都要考虑」| 状态：进行中

## 假设与判定规则（先登记后跑数）

- **H1（边际组合）**：某已评弱信号挂入 v9 31 特征板后，walk-forward
  IC 在 train 与 OOS25 双口径同向改善 → 该信号有组合价值（弱×强集合）。
- **H2（显式交互）**：弱×强、弱×弱构造的交互项作为新特征上板有效。
- **反向纪律**：与现有特征高相关（max|ρ|≥0.6）的弱信号基本无机会
  （e87 删特征先例）；多臂扫描存在假阳性 → 胜出必须双口径一致 +
  V9_PARAMS 全参复验 + 后续引擎评估。

## 第①步：冗余度解剖（已落地，`experiments/lab/e108/redundancy.json`）

21 个已评弱信号 × v9 31 特征 Spearman 截面相关（面板 578K 行，月均覆盖 12-99%）：

**高冗余组（max|ρ| ≥ 0.6，基本被现有特征覆盖——预期无机会）**
| 信号 | max\|ρ\| | 对应 v9 特征 |
|------|----------|-------------|
| e103f_fund_val 基金持仓市值 | 0.72 | fund_cov |
| e101_disp_cv 预测离散度 | 0.69 | s3_fy_slope |
| e106_fh_cnt 持基家数 | 0.68 | fund_cov |
| e103f_fund_n 基金家数 | 0.66 | fund_cov |
| e99_tp_gap 目标价缺口 | 0.61 | ret60 |

**中冗余（0.4–0.6，部分已被吸收）**：gdhs_chg 0.59(gdhs_qoq)、
gdhs_lvl 0.59(circ_mv)、n_orgs 0.49、org_n 0.46、cover_n 0.44、
hld_val 0.43(turnover20)、bert_pos_cnt 0.41(ev_fc_pos)

**正交候选（max|ρ| < 0.2——真正的组合候选池）**：
plg_* 质押三信号 (0.10–0.18)、disc_late 0.14、disc_resched 0.04、
depth_sh 0.13、cover_d 0.12、rating_chg90 0.12、fh_cnt_chg 0.10、
fh_mv_chg 0.10、rating_score 0.10、rev_breadth90/rev_net90 0.05、
bert_pos_den/neg_den/conf (0.25–0.32, 边界)

## 过程中的重要纠错（纪律留痕）

v10 首跑发现 ts_code 格式 bug（fh 特征 `sh.600000` vs Xlab `600000.SH`，
合并覆盖 0%）→ v10a/v10b 两臂喂零列结果逐位相同；v10a 表面"提升"
（+0.0067）实为零列引发 XGB 子采样路径漂移的**伪信号**，已修正映射
（覆盖率 98.6%）并重扫。教训登记：任何特征上板前必须先报合并覆盖率。

## 第②步：边际贡献扫描（运行中）

- 方法：`scripts/lab/e108_marginal_sweep.py`，每信号单独挂入
  V9_FEATS 走 e83 run_arm walk-forward（82 训练月 + 13 OOS 月）；
- 筛选参数 max_depth=8/n_estimators=300（~4x 加速），同参数 v9 基线对照；
- 21 个候选分两 shard 并行（s1: base+10, s2: 11）；
- 胜出者 → 回 V9_PARAMS 复验（v10 机制）→ H2 交互项 → 引擎评估。

## 第③步（待②结果）：显式交互项 + 模型内互证

对②胜出信号构造弱×强交互（如 crowding×turnover、late×mom20）再扫；
并用 XGB 特征重要性+树内共现确认边际改善非随机分裂噪声。

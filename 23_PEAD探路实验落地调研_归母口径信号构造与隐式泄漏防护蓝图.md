# 23 · PEAD 探路实验落地调研：归母口径信号构造、隐式泄漏防护与门禁对接蓝图

**编制日期**: 2026-09-15（CST）
**调研定位**: 把 Phase D 第三选项「PEAD 简化探路实验」从设计稿（`docs/PEAD_EXPLORATION_DESIGN.md`）落到**可执行蓝图**。
本报告是纯研究产物：⛔ 不动任何 `.py/.sh`，不改 git，不干预 tmux 会话。
所有数字均由真实数据底座实测得出（脚本见 §7 附录），非估计值。

---

## 0. 执行摘要：五条结论

1. **`PEAD_EXPLORATION_DESIGN.md` 的 S3 门槛存在量纲错误，会致 0 事件静默通过**。`debt_to_assets` 在 citydata `fina_indicator` 里是**百分数**（全池 p50=54.06、max=136.22），设计稿写 `debt_to_assets <= 0.80`，恒为假。实测首跑 `S1+S3 events: 0`。**必须改为 `<= 80`**。这不是设计选择，是 bug——且它恰好落在「零成交即失败」判据本该拦截的路径上：0 事件 → 0 成交 → 若有人把「零成交」误当作「低风险」，就会带着空报告过关。

2. **有效事件池比设计稿预期小一个量级**。修正量纲后实测 `S1（归母同比≥30%）+ S3（roe≥8 且 资产负债率≤80%）= 1828` 个事件（10 年 487 只），再过 F2（公告次日开盘跳空 ∈ [2%, 7.5%]）**只剩 122 个**，留存率 **6.7%**，远低于设计稿假设的 30–50%。**122 个事件是本蓝图的硬约束**：它决定了统计功效、持有期、参数自由度和门禁判据的全部设定。

3. **隐式泄漏的真正入口不是 pub_date 对齐，而是「公告后到信号日」的跳空归属**。`pub_date` 是自然日（实测周六公告占 17.9%，3026/16891），所以「次日」须定义为「下一个交易日」。但更大的问题是：**542 个事件（29.7%）公告到信号日相隔 >10 个自然日**，其中 532 个事件的标的**缺上一年日线 parquet**。这些是数据缺口（新股/分年缺失），不是真实停牌。若用这些标的的「信号日跳空」当作 PEAD 缺口，缺口里混的是停牌复牌补跌/补涨，而非公告反应——**这是连零前视对齐都挡不住的语义泄漏**。蓝图给出三道防护（§2）。

4. **S-3 流动性门禁在 2.5 万元单票口径下有真实超标风险**：F2 达标事件中 ADV2% 占比 p50=1.2%（安全）、p95=7.4%、max 16.3%。S-3 判据是「单笔委托 ≤ ADV 2%」，**2.5 万元口径下约 5% 的事件会超标**。这不是 PEAD 特有的，但必须在蓝图里显式处理（参与率参数），否则回测过、门禁红。S-1 换手门禁同样贴近上限（305%，见 §3.4），是 F2 上界与持有期 N 的联合约束。

5. **512890 ETF 的红利口径与本仓个股池不可直接复用**：其 `factor` 字段是**前复权**（raw 总收益 12.06%，复权 129.30%），而个股池 `data/dividend_stocks` 全池 `adjust_mode=RAW`。两池混用会触发 R4 复权口径红线。512890 可与宽度择时叠加，但**只在 ETF 独立的 RAW/前复权口径内闭环**，不与个股池混算（§5）。

---

## 一、归母口径下业绩公告事件信号的量化构造

### 1.1 数据底座实测（事实，非估计）

| 项 | 实测值 | 来源 |
|---|---|---|
| financial_pit | 487 只 × 16891 行 | `data/financial_pit/meta.json` + 全量 parquet 遍历 |
| 字段 | `code / pub_date / stat_date / roe / net_profit_yoy / debt_to_assets / cash_flow_per_share / source` | parquet schema |
| 对齐键 | `pub_date`（公告日，自然日） | meta.json `align_key` |
| 空值率 | net_profit_yoy 1287 空（7.6%）/ roe 111 / debt_to_assets 700 / cash_flow_per_share 1020 | 遍历 |
| 量纲 | roe=百分数（p50 3.4，p95 17.57）/ net_profit_yoy=百分数（p50 7.7，p95 347.2）/ **debt_to_assets=百分数（p50 54.06，max 136.22）**/ ocfps=元（p50 0.14） | 遍历 80 只抽样 |
| 月度分布 | 4 月 6349（37.6%）/ 8 月 3824 / 10 月 3978 / 3 月 1167 | 遍历 |
| 周内分布 | 周一 1202 / 周二 3388 / 周三 2667 / 周四 2936 / 周五 3672 / **周六 3026（17.9%）** | 遍历 |
| 日线池 | `data/dividend_stocks` 488（487 个股 + sh.000300），`adjust_mode=RAW`，2015–2024 分年 | meta + parquet |
| 宽度序列 | `breadth20_daily.parquet`，2431 有效日 2015-01-05～2024-12-31，含 ST，MA20 口径 | BREADTH_META.json |

**量纲证据（§0 结论 1 的依据）**：`debt_to_assets` 抽样 min=2.06 / p50=54.06 / p95=91.62 / max=136.22。百分数口径下 `<= 0.80` 恒假。设计稿 `PEAD_EXPLORATION_DESIGN.md` 第 32 行 S3 条件须修正为 `debt_to_assets <= 80`（即资产负债率 ≤ 80%）。

### 1.2 事件链定义（零前视，逐层）

```
公告日 pub_date（自然日，含周末）
   ↓  T = 第一个严格 > pub_date 的交易日      ← 唯一合法信号日（§2.1 防护 P1）
   ↓  在 T 日用 T 及以前全部信息判定 S1/S3/F1/F2
信号日 T
   ↓  T+1 次一开盘买入（引擎既有价格模型，不新增撮合规则）
买入日 T+1
   ↓  持有 N 个交易日（N ∈ {10, 20, 30}）
   ↓  次日开盘卖出
```

「严格 >」而非「≥」：公告当日的盘中时段可能尚未公开，用 `>` 保证 T 日收盘前信息已全部落地。这是对齐的**方向性正确**，但不是全部——见 §2。

### 1.3 信号条件与实测漏斗（S1+S3 真实数据）

| 层 | 条件 | 实测留存 | 留存率 |
|---|---|---|---|
| L0 | 全部公告行 | 16891 | 100% |
| L1 | `net_profit_yoy >= 30`（归母同比，百分数） | — | — |
| L2 | `roe >= 8`（百分数） | — | — |
| L3 | `debt_to_assets <= 80`（**百分数，修正后**） | — | — |
| **L1∧L2∧L3** | **S1+S3 基本面共振** | **1828** | — |
| L4 | 信号日 T 可定价（有日线、preclose>0） | 1828 | 100% |
| **L5** | **F2：`(open-preclose)/preclose ∈ [2%, 7.5%]`** | **122** | **6.7%** |
| L6 | F1 严格缺口 `low > preclose`（L5 子集） | 51 | 41.8% of L5 |
| 参照 | 一字板（open==high 且 gap>7.5%） | 44 | — |
| 参照 | gap>7.5% 全部 | 89 | — |
| 参照 | gap ∈ (7.5%, 9.5%] | 29 | — |
| 参照 | gap ∈ [0, 9.5%] | 765 | — |
| 参照 | gap>0 全部 | 825 | — |

**事件年份分布（1828）**：2015:128 / 2016:147 / 2017:195 / 2018:165 / 2019:146 / 2020:230 / 2021:298 / 2022:239 / 2023:168 / 2024:112。2021 年峰值 298，2024 年回落到 112——**事件密度在下降**，这与 2021 年「核心资产」业绩高增期吻合，也提示近年样本变薄。

**F2 上界 7.5% 的代价实测**：把上界从 7.5% 放宽到 9.5%，事件从 122 增至 151（+24%）。其中一字板 44 个必须排除（开盘=最高=涨停，无法合理买入，且 19 号报告 §3.3 明令禁止市价挂涨停抢单）。**建议把 F2 上界设为参数化 `gap_upper`（初值 7.5%，敏感性对照 9.5%）**，而不是写死——因为 7.5% vs 9.5% 的 24% 事件差是可观的样本成本，值得用对照来裁决。

**严格缺口 `low > preclose`（F1）在 L5 内只占 41.8%**：说明「跳空高开」与「日内不回补缺口」是两个不同强度的确认。F1 更严格但样本更薄。**建议 F1 与 F2 都作为独立开关**，跑四象限对照（F1on/F1off × 7.5/9.5），用 122→51 的样本代价换信号纯度，让数据裁决。

### 1.4 降级口径补偿：现金流污染率（必测，写进实验报告）

归母口径无法区分经常性与一次性收益。用 `cash_flow_per_share` 做最低限度反粉饰，**探路阶段必须给出两个数字**：

1. **污染率** = L1∧L2∧L3 达标样本中，`cash_flow_per_share` 同比 ≤ 0 的比例（经营现金流不支撑利润高增 ⇒ 嫌疑一次性收益）。
2. **分组漂移对比** = 把达标样本按 ocfps 同比正/负分两组，分别统计固定持有期漂移收益。

**裁决规则（沿用设计稿 §1.3，本蓝图不变更）**：
- 现金流正组显著更优 ⇒ 污染**可过滤** ⇒ 补采扣非字段收益有限，改为加 ocfps 过滤重跑。
- 两组无差异 ⇒ 污染已渗入信号 ⇒ **补采扣非字段是唯一出路**。

**ocfps 同比计算的数据条件**：ocfps 在 financial_pit 里是**当期每股值**，同比需要上一年同报告期值。financial_pit 每只票约 40 期（10 年×4 期），可按 `code + stat_date` 前推一年同季度取值。**注意前视方向**：比较的是「本期 ocfps vs 去年同期 ocfps」，两者在 T 日都已公告，无前视。但**必须用 stat_date 匹配历史期，不能用 pub_date 匹配**——否则同年提前公告的票会拿到「还没公告的下一期」。这是 ocfps 同比唯一的坑。

---

## 二、公告窗异常波动与 T+1 成交的隐式泄漏防护方案

这是本次调研的**核心新增内容**。设计稿只覆盖了「pub_date 零前视」这一层，实测发现还有三层隐式泄漏。

### 2.1 泄漏源盘点（四层）

| # | 泄漏源 | 实测证据 | 危害 |
|---|---|---|---|
| **P1** | pub_date 是自然日，「次日」若取日历日会指向非交易日 | 周六公告 3026 条（17.9%），周日公告在月度分布里体现为周一偏低 | 信号日错位，拿到错误的 K 线 |
| **P2** | **公告→信号日长尾延迟**：542/1828（29.7%）事件相隔 >10 自然日 | lag 分布：1 天 731 / 2 天 192 / 3 天 286 / 4 天 17 / 5 天 18 / 6 天 40；长尾到 300+ 天 | **最危险**：信号日的跳空可能是停牌复牌补跌补涨，不是公告反应 |
| **P3** | 长尾标的中 532/542 **缺上一年日线 parquet** | 如 `sz.301303` 只有 2023/2024 两年 | 数据缺口伪装成「停牌」，把数据残缺当成行情信号 |
| **P4** | 信号日 K 线本身已含公告反应（跳空高开 2%+） | F2 下界即 2% | 买入价含部分漂移，回测偏乐观（设计稿 R-9 已登记，本蓝图沿用） |

**P2/P3 的量化拆解（实测）**：
- `lag <= 3` 的事件：1209 个，其中 F2 达标 70 个（5.8%）
- `lag > 10` 的事件：542 个，其中 F2 达标 34 个（6.3%）
- 长尾组「缺上一年日线」：532/542

也就是说，**长尾组的 F2 达标率（6.3%）与短尾组（5.8%）几乎相同**——单看达标率分辨不出脏数据。必须用**显式的数据完整性断言**，而不是统计特征，来挡住 P3。

### 2.2 防护方案（三道，全部是纯判据，不依赖未来数据）

**防护 A：信号日必须由交易日历显式推导（挡 P1）**

```
signal_day(T) = min{ d ∈ trading_calendar | d > pub_date }
```
trading_calendar 复用 `finai/sources/citydata_source.py` 的 `trade_cal`（`is_open=1`），与 T105 采集器同一来源。**禁止用 `pub_date + timedelta(days=1)`**。 weekends 落到下周一，长假（春节/国庆）自动跳过。

**防护 B：数据完整性断言，fail-closed（挡 P2/P3）**

在信号判定前对每个候选事件做三条断言，任一失败**丢弃该事件并计数**（不 raise，不阻断回测，但必须写进实验报告的 `events_dropped`）：

| 断言 | 规则 | 目的 |
|---|---|---|
| B1 | 信号日 T 的 K 线存在，且 `preclose > 0`，且 `open/high/low` 有限 | 基本可定价 |
| B2 | **T 日前 ≥ 20 个交易日连续 K 线全部存在**（无缺口） | 保证 MA20/ADV20 可算，且 T 不是复牌首日 |
| B3 | **`lag(T - pub_date) ≤ 3` 个自然日**（初值，可配置） | 挡长尾：公告反应窗口已过，跳空归属不可信 |

**B3 是本蓝图对设计稿的关键修改**。设计稿没有 lag 上限，隐含假设「所有公告都能在下一个交易日反应」。实测 29.7% 的事件违反这个假设。`lag<=3` 的初值依据：lag=1/2/3 覆盖 1209/1828 = 66.2% 的事件，且覆盖全部「正常交易日次一/次二/次三日」语义（含周末顺延到下周一的 3 天）。`lag>3` 的 619 个事件中，绝大多数是数据缺失（532 个缺上一年日线），真停牌的是少数。

**B3 的代价**：丢弃 619 个事件（33.8%），其中 34 个 F2 达标（占 122 的 27.9%）。**这是用样本换可信度**。蓝图建议 `max_lag` 参数化（初值 3，敏感性对照 5、10），让代价可见、可裁决。

**防护 C：RAW 口径显式断言（挡 P4 的复权变体 + R4 红线）**

```
assert bar.adjust_mode == "RAW"
```
缺口必须在 RAW 序列上算。除权日 preclose 与当根 K 线同属未复权序列，缺口比较有效；若混入复权序列，除权日会产生假跳空（母库 R4 复权口径红线）。`data/dividend_stocks` 全池 RAW，但**必须在实验代码里显式断言**，防止未来数据层变更。

### 2.3 T+1 成交与宽限期的对接（不新增撮合规则）

买入：T 日信号 → T+1 次一开盘成交（引擎既有 `price_model`，T204）。
卖出：持有 N 日后次日开盘成交。

**S-2 门禁的 T+1 宽限（既有机制，直接复用）**：`gate_s_scientific.py:220-229` 已实现 `timing_grace_dates`——避险信号日 T 下单、最快 T+1 成交，段首日的盘中持仓属真实成交滞后而非死扛，由 run 脚本按交易日历计算后传入。**PEAD 探路实验在宽度冰点档的清仓同样走这条路径**：冰点信号日 T 清仓 → T+1 成交，段首日宽限，不新增任何宽限逻辑。

**缺口买入价偏乐观（P4，沿用设计稿 R-9 缓解）**：F2 要求开盘已跳空 ≥2%，回测的开盘买入价已含这部分漂移。T204 敏感性分析结论是「价格口径差异属二阶小量，滑点主导」，故**不新做价格模型**，挂 T204 九宫格对照即可。若探路结果 CAGR 卡在 +5% 门槛附近（4–6%），则必须重算去掉缺口后的净漂移，再裁决。

---

## 三、信号产出与 S-2 / S-4 门禁的对接设计（不绕防病规则）

### 3.1 对接原则：只复用既有 context 契约，不改门禁代码

S-Gate 的五个门禁全部接受 `dict` context（`gate_s_scientific.py:104-114`），**判据在产出侧（run 脚本）计算后传入**。PEAD 探路实验只负责**产出口径正确的 context 字段**，不改任何 `evaluate()`。这是「不绕防病规则」的核心：门禁语义不变，PEAD 只是新的 context 生产者。

### 3.2 S-2（择时空仓生存，BLOCKER）对接

**S-2 已是双口径门禁**（宽度优先，MA200 兜底，`gate_s_scientific.py:90-263`）：

| 字段 | PEAD 探路必传 | 说明 |
|---|---|---|
| `use_breadth_timing` | `True` | 宽度口径优先分支 |
| `breadth_series` | `data/market-breadth-a/breadth20_daily.parquet` 全序列 | **禁止 None**：宽度开启但缺序列 → INCONCLUSIVE（fail-closed，:142-153） |
| `breadth_defense_threshold` | `0.20` | 与 `DividendConfig` 一致 |
| `daily_positions_ratio` | 逐日持仓市值/NAV | **必须覆盖每个冰点日**，缺一日即 INCONCLUSIVE（:199-218） |
| `timing_grace_dates` | 按交易日历算的段首宽限日 | 见 §2.3 |

**S-2 的三个 fail-closed 陷阱，PEAD 全部会撞上**：
1. `breadth_series=None` → INCONCLUSIVE（不是 SKIP）。
2. 冰点日缺 `daily_positions_ratio` → INCONCLUSIVE。**PEAD 持仓稀疏（122 事件/10 年，大部分时间空仓），空仓日的 ratio=0.0 必须显式写入**，不能省略——省略即「证据不足」。
3. 段首日未宽限且持仓 >5% → FAIL。PEAD 在冰点档的清仓依赖 T+1 成交，**必须由 run 脚本算出 `timing_grace_dates` 传入**，缺省不再豁免（:220-225）。

**关键判定**：S-2 阈值是「持仓比例 ≤ 5%」，而 PEAD 是**集中持仓 3–5 只、单票 ~25%**。**PEAD 在冰点档必须全额清仓，不存在「低仓避险」的中间态**。这与 19 号报告 §2.1 的「冰点全额空仓」一致，也与 S-2 的 5% 容差兼容（清仓后 ratio=0 ≤ 5%）。**PEAD 探路实验的冰点行为 = 全部止损离场，沿用既有 `diff_to_orders(current, {}, ...)` 路径，零新增逻辑。**

### 3.3 S-4（红利税避税锁定期，CRITICAL）对接

S-4 判据（:425-437）：`penalty_tax_amount / total_dividend_received <= 0.20`。**PEAD 与 S-4 的关系是结构性的**：

| 情形 | PEAD 行为 | S-4 结果 |
|---|---|---|
| 持有 N=10 天，期间遇除权 | 持股不满 1 个月（30 天）→ 20% 惩罚性红利税 | **惩罚税占比极高，S-4 必红** |
| 持有 N=10 天，期间无除权 | 无分红入账 | `total_div <= 0` → **INCONCLUSIVE**（:401-411，无证据≠通过） |

**这是 PEAD 探路最容易被误判的地方**：N=10 的短持有期 + 分红登记日前 15 天的「装甲一」过滤（19 号报告 §3.1，TASK_TRACKER P7），会让大部分交易「无分红入账」，S-4 直接 INCONCLUSIVE。**INCONCLUSIVE 不是 PASS**，不能当作合规。

**蓝图给出三条路，按优先级**：

1. **首选：装甲一前置过滤（事前拦截，非事后门禁）**。在信号判定阶段读 `data/dividend_stocks/exdiv/{symbol}.parquet`（已核实：date/factor/cash_dividend 三字段全池齐备），若候选股未来 15 个交易日内有除权除息，**一票否决禁止买入**。这样 PEAD 持仓期间根本不会遇到分红，`total_dividend_received=0`，S-4 判定为 SKIP（:384-394，`if not context` 分支）而非 INCONCLUSIVE——**这是唯一能让 S-4 干净通过的路径**，且它就是 19 号报告 V5-ALPHA-03 的原设计。TASK_TRACKER P7 已把「装甲一 + S-4 事前拦截化」列为待办，本蓝图确认该待办是 PEAD 的**前置依赖**。

2. **若不实现装甲一**：必须实测 `penalty_tax_amount` 与 `total_dividend_received` 并把 N 调到 ≥30 天，否则 S-4 必红。但 N=30 会让事件数从 122 进一步摊薄。

3. **禁止**：关闭红利税作弊（S-3/S-5 明令禁止，S-5 会查印花税/佣金是否为零）。**不要考虑 `enable_dividend_tax=False` 绕过。**

**S-4 与持有期 N 的耦合结论**：N ∈ {10, 20, 30} 三档中，**只有 N=30 有可能同时满足 S-4 与事件密度**；N=10/20 必须配合装甲一。建议探路主跑 N=20 + 装甲一，敏感性对照 N=10/30。

### 3.4 其余相关门禁（S-1/S-3/S-5/L-1）

| 门禁 | 判据 | PEAD 探路的实测风险 | 对策 |
|---|---|---|---|
| **S-1** 换手 <400%（年化单边） | BLOCKER | 122 事件/10 年、N=20：年化单边换手 = (12.2 往返/年 × 2 × 250/20) ≈ **305%**；放宽 F2 到 9.5%（151 事件）≈ **377.5%** | **贴近上限但不超**。305% 距 400% 只剩 95pp 缓冲，任何提高事件密度或缩短 N 的调整都可能越线 ⇒ S-1 是 F2 上界与 N 下界的**联合约束**，须实测不可假设；参与率参数沿用 `PortfolioConfig` 既有 5% |
| **S-3** 单笔 ≤ ADV2% + 滑点+50% 后收益>0 | CRITICAL | 2.5 万元单票口径：p50 1.2%、**p95 7.4%、max 16.3%** | 约 5% 事件超标。用参与率参数（`PortfolioConfig` 既有 5%）或下调单票金额到 2 万元；滑点压测沿用 T204 的 15bps 档 |
| **S-5** 归因证据链 | BLOCKER | 零成交会触发「成交笔数=0 但费率为零」的 INCONCLUSIVE（:498-514） | 实验报告必须带 `code_evidence`（代码行号 + 账本对账单），与设计稿 §5.3 一致 |
| **L-1** 声明启用红利税 → 必须有 DIVIDEND_TAX 流水 | — | 若装甲一生效，分红为 0，`Count(DIVIDEND_TAX)=0` | **L-1 判据是「声明启用→必须有≥1条」**。装甲一与 L-1 直接冲突：要么不开红利税（但 S-3/S-5 禁止关税作弊），要么接受冲突。**这是蓝图识别出的门禁间张力，须用户裁决**（见 §6 待确认 3） |

### 3.5 产出位置与出处三件套

- 实验产物一律落 `experiments/lab/pead-probe-*/`，经 `scripts/lab/run_experiment.py` 生成 `experiment.json` + leaderboard 条目。
- **⛔ 绝不写入权威目录 `experiments/runs`**（lab 纪律，设计稿 §4.1 已定）。
- 出处三件套（G-1）：Git SHA = `git rev-parse HEAD`（当前 `c944917`）；数据哈希 = 对 `data/financial_pit/` + `data/dividend_stocks/` + `data/market-breadth-a/` 三目录取 SHA-256（financial_pit meta.json 已含逐票 sha256，可直接复用为细粒度指纹）；时间戳 = 实验运行时刻。

---

## 四、探路实验可执行蓝图（最小闭环）

### 4.1 参数与自由度（严格控制）

| 参数 | 初值 | 敏感性对照 | 备注 |
|---|---|---|---|
| `yoy_threshold`（S1） | 30 | 40 | 19 号报告原口径是扣非≥30；归母降级是否抬到 40 待裁决（§6 待确认 1） |
| `roe_min`（S3） | 8 | 12 | 百分数 |
| `debt_to_assets_max`（S3） | **80**（⛔ 修正量纲） | 70 | **设计稿的 0.80 是 bug** |
| `gap_lower`（F2） | 0.02 | — | 与 F1 跳空高开下界对齐 |
| `gap_upper`（F2） | 0.075 | 0.095 | 上界受 S-1 换手约束（§3.4） |
| `use_strict_gap`（F1） | True | False | 四象限对照 |
| `max_lag`（防护 B3） | 3 | 5, 10 | **新增参数**，挡长尾泄漏 |
| `holding_days` N | 20 | 10, 30 | N=10/20 须配合装甲一才过 S-4 |
| 宽度阈值 | 0.20 / 0.40 | **不在本实验调参范围** | 已有独立网格任务（TASK_TRACKER finai-breadth-grid） |

**过拟合防护**：自由度共 7 个（yoy/roe/debt/gap_lower/gap_upper/max_lag/N）。沿用 `strategy/param_scan.py::ParamScan` 单参数 ±20% 扰动 + 悬崖判定（CAGR 翻负 / MDD≥基准×2 且>5% / 零成交）。**基线先悬崖即炸，不允许带病扫描**。宽度阈值**不纳入调参**。

### 4.2 五步闭环

| 步 | 内容 | 复用 / 新增 |
|---|---|---|
| 1 | 事件对齐：`signal_day = min{d ∈ trade_cal | d > pub_date}` + 防护 A/B/C | 新增纯判据（§2.2），交易日历复用 citydata `trade_cal` |
| 2 | 信号：S1+S3+F1+F2 | 新增筛选，不涉及撮合 |
| 3 | 持有与退出：固定 N 日，次日开盘卖出 | 复用引擎时间退出，不新增止损 |
| 4 | 择时：宽度三档（进攻满仓 / 警戒不新建 / 冰点清仓） | **完全复用** `DividendStrategy.on_bar` ③.5 段状态机；PEAD 只实现进攻档持仓生成器 |
| 5 | 产出：`experiments/lab/pead-probe-*/` + 出处三件套 | 复用 `scripts/lab/run_experiment.py` |

### 4.3 样本量与统计功效（必须先说清，避免伪显著）

- 可交易事件 **122 个**（F2 主路径，max_lag=3 后约 88 个）——**比设计稿估计的 600–1700 小一个量级**。
- 122 事件、胜率 45% 估计 ⇒ 95% 置信区间半宽 ≈ ±8.8pp。**「50% vs 45%」在这种样本下无法显著区分**。
- **结论：探路的统计功效不足以支持细粒度参数裁决**。蓝图据此把验收门槛设为**「粗粒度方向性判据」**，见 §4.4。

### 4.4 验收门槛（按 122 事件量级重定，替代设计稿 §5.2 的 400 事件口径）

设计稿 §5.2 的事件数下限是 400，实测只有 122。**门槛必须按实测下调，且必须显式声明折价**：

| 裁决 | 条件 | 动作 |
|---|---|---|
| **值得补采扣非** | CAGR ≥ +5% **且** MDD ≤ 35% **且** 相对红利宽度基线 CAGR 差 ≥ +3pp **且** 事件数 ≥ 80 | 立项补采 citydata `fina_indicator` 扣非同比字段，重跑同口径 |
| **先修信号** | CAGR 差达标但绝对值未达 +5%，**或** ocfps 分组对比显示污染组显著更差 | 加 ocfps 过滤重跑，不补采 |
| **方向存疑** | CAGR < 0，或 MDD > 35%，或相对基线差 < +1pp | 弃案或转 512890；**不补采** |

**门槛依据（每条可追溯）**：
- **CAGR ≥ +5%**：19 号报告 §0 的指数型 ETF 宽基「平均年化 5%~8%」是本项目机会成本下限。广发 20% / 中泰 29.88% 是**扣非口径 + 全市场选股域**的上限参考，不是达标线。
- **MDD ≤ 35%**：T313/G4.5 红利策略硬门槛（项目系统级风控）；宽度基线 MDD 22.49%（S-2 违规 35 天不可作基准）～40.46%，35% 是有实证锚点的中间值。
- **事件数 ≥ 80**：122 事件在 max_lag=3 后约 88 个，80 是「样本有效性下限」的折价设定（设计稿原 400 是基于错误估计）。
- **置信区间而非点估计**：报告必须给出胜率/收益的 95% CI。

### 4.5 不做的事（探路边界）

不做：SUE 因子（487×10 年样本不足以估 σ）；量能放大（需额外 MA5）；五重过滤全量；2% 价格笼子限价模拟（引擎已次一开盘成交，笼子在模拟盘阶段才有意义）；扣非字段补采（**这正是探路要决定的事，先做就是循环论证**）；警戒档 PEAD 仓位上限调参（mid_cap 网格独立任务）。

---

## 五、512890 红利低波 ETF：标的、红利口径、与宽度择时叠加

### 5.1 标的实测

| 项 | 值 | 来源 |
|---|---|---|
| 名称 | 红利低波 ETF（512890.SH） | `scripts/collect_etf_data.py:49` |
| 上市日 | **2019-01-18**（前段合法缺失，非数据缺口） | `collect_etf_data.py:49` + parquet 实测 |
| 可用区间 | 2019-01-18 ～ 2024-12-31，1443 个交易日 | parquet 遍历 |
| schema | date/open/high/low/close/volume/amount/**factor**/code/source/adjust_mode | parquet schema |
| 复权口径 | **factor 为前复权**（raw 总收益 12.06%，复权 129.30%） | 实测 |
| 因子数 | 2 档（1.0 → 2.0462） | 实测 |

### 5.2 红利口径的关键结论：不可与个股池混用

**512890 的 `factor` 是前复权**，而个股池 `data/dividend_stocks` 全池 `adjust_mode=RAW`。两池混用会触发母库 **R4 复权口径红线**（`finai/sources/adjustment_mode.py`）。

**处理原则**：512890 的一切计算（MA200、收益率、跳空）**只在 ETF 自己的口径内闭环**：
- 用前复权价（`close × factor`）算 MA200 与收益率 —— 前复权保证了分红再投资的总回报语义，129.30% 是含分红的真实总收益。
- 若要与个股池同框比较，**必须用 RAW 收益率**（12.06%），并在报告里声明两种口径的差异。

**ETF 无红利税问题**：ETF 分红不征收个人红利税（基金分红免税），**S-4 门禁在 512890 上天然不适用**（`total_div=0` → SKIP 或 INCONCLUSIVE，取决于 context 是否传入）。这是 512890 相对个股红利策略的**结构性优势**：不需要装甲一、不需要 S-4 拦截。

### 5.3 收益实测（前复权口径，含分红再投资）

| 年 | 512890 前复权收益 | 备注 |
|---|---|---|
| 2019 | +20.84% | 上市首年（1/18 起） |
| 2020 | +8.14% | |
| 2021 | **+23.24%**（raw −39.77%） | **raw 与复权差 63pp**：2021 年大额分红 + 贴权 |
| 2022 | +1.11% | |
| 2023 | +11.19% | |
| 2024 | +21.51% | |
| **全区间** | **+129.30%**（约 6 年，年化 ≈ +14.7%） | 同窗口 510300 +27.04% |

**⚠️ 这里的 +129.30% 必须打折解读**：
1. **只有 6 年**（2019-01-18 起），而个股回测是 10 年（2015–2024）。**窗口不可比**。2015–2018 是股灾+熔断+蓝筹熊市，512890 的 6 年恰好避开了最差的几年。
2. **前复权因子只有 2 档**，说明期间只有一次大额分红事件（或一次集中调整）。**样本点太少，129.30% 的「含分红总收益」可信度有限**。
3. 510300 同窗口 +27.04%，512890 前复权 +129.30% —— 差距 102pp 主要来自 2021 年的 63pp 复权差。**这一年的因子调整是单点事件，不能外推**。

**结论**：512890 的真实吸引力应该用 **raw 口径 + 分红现金流单独记账**来评估，而不是直接信 129.30%。蓝图建议：若走 512890 方向，**另起一个 ETF 专用回测**，分红作为现金流入账（而非复权因子），用 `backtest/metrics.py` 的既有费用/分红透视。

### 5.4 与宽度择时的叠加：可行，且比个股更干净

| 项 | 512890 + 宽度 | 说明 |
|---|---|---|
| 宽度序列覆盖 | ✅ `breadth20_daily.parquet` 2015-01-05～2024-12-31，**完整覆盖** 512890 全部上市期 | 无对齐缺口 |
| MA200 叠加 | 前复权价 73.9% 时间在 MA200 上方 | 可做「MA200 滤网 + 宽度冰点空仓」双滤网 |
| ADV / S-3 | 5 万元单笔 / ADV20：p50 0.63%，**max 17.1%** | 尾部仍有超标，但中位数远优于个股（1.2% vs 0.63%） |
| 换手 / S-1 | 买入持有 → 年化换手 ≈ 2/6 年 ≈ **0.06%** | **远优于个股方案**，S-1 完全无压力 |
| S-2 | 宽度冰点空仓，走既有 `timing_grace_dates` | 与个股方案同一机制 |
| S-4 | **不适用**（ETF 分红免税） | 结构性优势 |
| S-3 滑点压测 | 需实测 | 买入持有换手极低，滑点影响 ≈ 2 次交易/6 年，几乎为零 |

**叠加设计建议**：512890 与宽度择时的组合是**三选一里工程成本最低、门禁冲突最少的**。它把「个股选股 alpha」换成了「ETF beta + 择时」，放弃了 19 号报告的个股高弹性，但换来：
- S-1（换手）零压力
- S-4（红利税）不适用
- S-3（流动性）中位数 0.63%
- 选股域偏窄问题（设计稿 R-3）消失

**代价**：没有个股 alpha，收益取决于 ETF 本体 + 择时。raw 口径 12.06%/6 年（年化约 1.9%）+ 择时增强，**可能达不到 CAGR +5% 门槛**。这是 512890 方向的核心风险，必须用实测回答，不能假设。

---

## 六、待人工确认事项

1. **S1 门槛初值 30% 是否认可**：19 号报告原口径是扣非≥30%。归母降级口径是否应抬到 40%（等同把拥堵期补偿推广到全期）？（沿用设计稿待确认 1）
2. **`max_lag` 新增参数初值 3 是否认可**：这丢弃 33.8% 的事件（其中 34 个 F2 达标，占 27.9%）。这是本蓝图相对设计稿最大的修改，须拍板。
3. **L-1 与装甲一的门禁张力如何裁决**：装甲一使 PEAD 持仓无分红入账，L-1「声明启用红利税→须有 DIVIDEND_TAX≥1 条」会与之冲突。选项：(a) 装甲一 + 接受 L-1 INCONCLUSIVE 并在报告登记；(b) 不开红利税（违反 S-3/S-5，不推荐）；(c) 只在 N=30 档开红利税。须用户裁决。
4. **S-1 换手约束是否作为 `gap_upper` 与 N 的联合约束**：实测 122 事件/10 年/N=20 估算年化单边换手 ≈305%（距 400% 上限剩 95pp），放宽 F2 到 9.5%（151 事件）≈377.5%，缩短 N 到 10 会翻倍到 ≈610% 越线。是否认可「F2 上界与 N 下界由 S-1 联合反定界」？
5. **选股域是否扩充**（沿用设计稿 R-3）：当前 487 只是红利池，PEAD 经典牛股（沪电股份、长春高新、广和通）多为成长股，可能系统性不在本池。若探路「方向存疑」，是否允许扩充到全市场重跑一次再定弃案？
6. **512890 方向是否要另起 ETF 专用回测**：本蓝图建议用 raw + 分红现金流记账，而非前复权 129.30%。若要走 512890，须先认可这个口径。
7. **启动时机**（沿用设计稿待确认 6）：TASK_TRACKER P8 标注「启动时机见决策记录」。当前宽度参数网格运行中，网格期间禁止改动 strategy/scripts/backtest/tests。PEAD 探路不可避免要动 strategy/，**须等网格结束、冠军组合固化后再启动**。启动条件是「网格完成 + 基线组合晋级」还是另行指定？

---

## 七、附录：本报告数字的实测方法（可复现）

所有数字由以下探查脚本产生（一次性运行，结果写入本报告；**非回测，不落盘实验产物，不写 git**）：

```python
# 事件链实测（§1.1/§1.3/§2.1）
# 遍历 data/financial_pit/*.parquet（487 文件）
#   → 过滤 S1(net_profit_yoy>=30) ∧ S3(roe>=8, debt_to_assets<=80)
#   → signal_day = 第一个严格 > pub_date 的交易日（data/dividend_stocks 日线序列）
#   → 计算 (open-preclose)/preclose 分档统计
#   → 记录 calendar lag 分布
# 关键修正：debt_to_assets 量纲 = 百分数（非小数），<=0.80 恒假
```

```python
# ADV / S-3 实测（§3.4）
# 对 F2 达标事件取前 20 日 amount 均值作 ADV20
# 单笔 25000 元 / ADV20 → p50 1.2%, p95 7.4%, max 16.3%
```

```python
# 512890 实测（§5）
# data/etf_bars/sh.512890/*.parquet 6 年合并
#   → raw 总收益 12.06%，close×factor 总收益 129.30%
#   → 逐年 raw / 复权收益对比
#   → 前复权 MA200 上方占比 73.9%
#   → 5 万元/ADV20: p50 0.63%, max 17.1%
```

**数据出处三件套**：
- financial_pit: collection_date `2026-09-08T00:00:55`，citydata(fina_indicator)，487 只 × 16891 行，meta.json 含逐票 sha256
- dividend_stocks: tencent RAW 不复权，488 只，2015–2024 分年
- market-breadth-a: git_sha `0f8471f`，source_data_hash `8bfa3aa9a74a64fc`，2431 有效日
- etf_bars/sh.512890: citydata fund_daily × fund_adj，2019-01-18 起
- 代码基线: FinAI2.0 `c944917`（HEAD）

---

## 八、与既有设计稿的差异清单（供设计稿修订）

| # | 设计稿（PEAD_EXPLORATION_DESIGN.md） | 本蓝图 | 依据 |
|---|---|---|---|
| 1 | S3 `debt_to_assets <= 0.80` | **`<= 80`（量纲修正）** | 实测 p50=54.06，max=136.22；原值致 0 事件 |
| 2 | §4.2 估计事件 600–1700 | **实测 122**（F2 主路径） | 全量遍历 |
| 3 | §5.2 事件数下限 400 | **下调到 80** | 实测样本量 |
| 4 | 无 lag 上限 | **新增 `max_lag=3` 防护 B3** | 29.7% 事件 lag>10，532/542 缺上年日线 |
| 5 | §4.2 未提 S-1 换手约束 | **S-1 联合定界 F2 上界与 N 下界** | 估算 N=20 时年化换手 ≈305%（F2 放宽到 9.5% ≈377.5%，N=10 ≈610% 越线） |
| 6 | §3.2 未提 S-4 与 N 的耦合 | **N=10/20 须配装甲一** | S-4 惩罚税判据 + L-1 张力 |
| 7 | 未识别 L-1 与装甲一冲突 | **登记为待裁决张力** | §3.4 / §6 待确认 3 |
| 8 | F2 上界 7.5% 固定 | **参数化 `gap_upper`（对照 9.5%）** | 7.5→9.5 增 24% 事件，但受 S-1 约束 |
| 9 | §5.3 未含 512890 ETF | **新增 §5 完整口径** | 本报告新增 |

---

## 来源与检索记录

1. `docs/PEAD_EXPLORATION_DESIGN.md`（FinAI2.0 代码仓，设计稿基线）；
2. 19 号报告《A 股个股高 Alpha 进攻体系深度调研报告》（research-finai-latest，PEAD 与宽度择时原始论证）；
3. 17 号报告《中低频量化研发防伪与工程质量门禁体系深度调研报告》§4.1（D/L/E/A/S/G 门禁判据速查表）；
4. `scripts/gates/gate_s_scientific.py`（S-1~S-5 机读实现，S-2 双口径分支 :90-263、S-4 :370-449、S-5 :452-537）；
5. `data/financial_pit/meta.json` + 全量 parquet（487 文件，本报告全部财务字段实测来源）；
6. `data/dividend_stocks/`（488 只 tencent RAW 日线）+ `data/dividend_stocks/exdiv/`（除权 date/factor/cash_dividend）；
7. `experiments/lab/market-breadth-a/BREADTH_META.json`（宽度序列口径）；
8. `data/etf_bars/sh.512890/` + `scripts/collect_etf_data.py`（512890 上市日与 factor 口径）；
9. `docs/TASK_TRACKER.md` P7/P8（装甲一与 PEAD 探路立项状态）；
10. `docs/HANDOFF_20260915.md`（Phase D 三选一决策上下文，已标记 SUPERSEDED）。

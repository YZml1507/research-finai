# 第三轮调研更新启动提示词（复制全文转发给新窗口即可）

> 生成日期：2026-08-28｜用途：在全新会话窗口执行第三轮“保鲜复查+待验证定稿”。
> 与第二轮的关系：不是重做调研，而是执行 14 号台账的既定节奏 + 把悬而未决项定稿 + 为 Phase 0-1 实测铺路。

---

我在做一套 A 股中低频量化交易系统的预研（个人开发者、预算有限、日线~小时级信号、持仓数日至数月）。此前两轮调研已产出交付体系，全部在 D:\Projects\research-finai：00_README 导航（V3）、01-08 为两轮修订后的文档（各自文末有修订日志）、09 为第二轮启动提示词存档、10 为第二轮审阅与补盲报告（含 27 条核验总表与待验证台账 T1-T10）、11 为程序化交易监管细则与报备实操、12 为免费行情数据源实测对比与选型指南（含 Phase 1 统一实测方案）、13 为回测框架 A 股适配代码级对比（含 5 个必挂用例）、14 为易变数据保鲜与复查台账（A-E 五本字段化台账+复查 SOP）。

现在你在新窗口做第三轮调研更新，四个任务：

**任务一：执行 14 号台账的既定复查（SOP 在 14 号文档 §一，逐行照抄执行）**
- 月度项（A1 印花税/A2 过户费/A6 融资利率/A7 两融余额/D11 akshare 版本）距上次核验已满一个月周期，全部复查一遍；
- 季度项里挑与“新窗复核”高价值的做：B1 涨跌停档位、B5 保证金比例、C1/C2 程序化交易时间线与高频线、D1-D9 九项目 star/版本快照（上次 2026-08-27，shields.io 或 GitHub 页直读）；
- 每条按台账格式回填“最近复查日”，有变更的走来源文档修订日志（改了什么/为什么/依据来源），无变更只更新台账 §七 复查记录表；
- 快照类数据（两融余额/利率区间/star 数）一律标注本次检索日期，禁止沿用旧日期。

**任务二：把 T1-T10 待验证项尽量定稿（清单在 10 号报告 §八，台账 E 同步追踪）**
优先攻这几个免费渠道可达的：
- T1 证管费 0.002% 官方直达原文（试中国结算/证监会法规库站内检索）；
- T2 过户费“低于 0.01 元按 0.01 元计”是否存在（中国结算收费通知全文）；
- T3 2025 年主动退市家数（5 家 vs 6 家来源冲突，找 21 财经 2026-01-19 原文）；
- T4 上交所 2026 交易规则全文（试静态 PDF 或 wayback 快照）；
- 定稿动作：查实的改来源文档+修订日志；仍查不到的维持“待验证”并更新触发条件；来源冲突不强行改判。

**任务三：为 Phase 0-1 实测启动做支持（只做调研准备，不写交易系统代码）**
- 检查 12 号文档第 9 章统一实测方案的股票池/区间设计是否仍成立，若有数据源接口变动（如 akshare 接口失效）更新 12 号；
- 摸底本机环境：Python 版本、pip 可用源、是否需要代理（本次会话可用 http://127.0.0.1:7897 作为 HTTP/HTTPS 代理访问外网）——把“环境准备清单”写进 12 号的附录或独立小节，供我 Phase 0 直接照做。

**任务四：可用工具说明（重要，按优先级执行）**
1. 首选：autoglm web_search / open-link（免费、已在既有文档链路里跑通，来源格式与 10 号报告一致）；
2. 备用网页通道：TinyFish——已配置为备用浏览器（搜索+正文抓取）。直接命令：`tinyfish search query "<q>" --include-domains <域>` 与 `tinyfish fetch content get "<url>"`（npx -y @tiny-fish/cli@latest 调用；外网走代理 http://127.0.0.1:7897；MCP 工具需重启会话后加载，但 CLI 直连命令立即可用；其 skill 见托管技能目录 use-tinyfish）。适用场景：autoglm open-link 被反爬挡住、或需要第二抓取通道交叉验证时。
3. 备用/并行：monid CLI——第三方数据端点聚合工具。其 skill 已安装为本机托管 skill（C:\Users\MengLin\.openclaw-autoclaw\skills\monid\SKILL.md，新窗口可直接读到；原文 https://monid.ai/SKILL.md ）。所有外网访问走代理 http://127.0.0.1:7897 。它走用户余额计费，**只在这些场景考虑使用**：autoglm 链路拿不到的数据（如交易所页面动态加载正文、社交媒体/API 级数据）、或需要与 autoglm 交叉验证的可疑数据。使用前先 `monid discover` 看有没有对口端点，跑之前用 `monid balance` 确认余额并控制调用量（小 limit 试跑），把产生的成本记进复查记录表备注。若 monid 未安装或没有 API key，跳过它，不要为此停下——autoglm 链路够用。
4. 禁止：付费购买任何数据源、注册任何新账号（monid 已有 key 可直接用；TinyFish 已有 key 已写入配置）。

**纪律与产出要求**（与第二轮一致）：
- 关键结论 ≥2 独立可追溯来源，时效数字一律标注本次检索日期；
- 修改来源文档必须在其文末修订日志登记（改了什么/为什么/依据来源）；台账回填不算修订，但也必须留痕（§七 记录表）；
- 对既有文档不重写整体框架，只做数据刷新与定点修订；
- 同步更新 index.html 总览页（补第三轮区块）；如有重大制度性变更，在 05 号追加第三轮记录小节；
- 最后更新 00_README 导航（第三轮入口）与 14 号台账，并给我一份第三轮交付说明（改了哪些文档/台账回填结果/待验证项定稿情况/Phase 0-1 环境准备清单）。

---

## 转发后新窗口将做什么（对照表）

| 新窗口任务 | 对应文件 | 预期产出 |
|---|---|---|
| 台账月度+重点季度复查 | 14 号 §一/§二/§三/§五 | 复查记录表新增行 + 数据变更时修订日志 |
| 待验证项定稿 | 10 号 §八 T1-T10 | 定稿项改文档+关台账；未定稿项更新触发条件 |
| Phase 0-1 环境摸底 | 12 号 §9 + 新附录 | 环境准备清单（Python/代理/数据源接口现状） |
| README 与台账收口 | 00_README / 14 号 | 第三轮入口 + 复查记录完整 |

## 外部通道优先级与护栏（新窗口必读）

- **优先级**：① autoglm web_search/open-link（免费，主力）→ ② TinyFish（备用网页抓取/搜索，免费额度内）→ ③ monid（计费数据端点）→ ④ 都拿不到就登记待验证；
- **monid 是备用，不是主力**：autoglm 免费链路（web_search/open-link）是第一优先级，monid 只在 autoglm 拿不到或需要交叉验证时用；
- **它花钱**：monid 走用户余额（app.monid.ai 注册+API key），每次 run 都计费——小 limit 试跑、控制调用量、成本记入复查记录表；
- **它需要外网**：走代理 http://127.0.0.1:7897；若 CLI 未安装（npm install -g @monid-ai/cli）或没有 API key，直接跳过，不要阻塞主线；
- **不要为它注册账号**：没有 key 就跳过，任务三、四在 autoglm 链路下照样可完成。

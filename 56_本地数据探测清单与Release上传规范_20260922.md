# memo 56：本地数据探测清单 + Release 上传规范

## A. 待探测数据清单（按优先级）

### ① 分析师一致预期历史（最高优先，付费清单①号的免费兜底探测）

**要什么**：每只股票**每日/每月截面**的一致预期 EPS、一致预期
净利润、综合评级、目标价——关键是"历史快照"（能回看 2018 年
1 月 5 日那天市场的一致预期值），只给当前值的不要。
必须含退市股（PIT 纪律）。

**本地探测路径**：
- 终端类：同花顺 iFinD"盈利预测/一致预期"、东财 Choice
  "盈利预测"、Wind 试用版——看能否按**历史日期**导出全 A 截面。
- Python：`akshare` 试 `stock_profit_forecast_em(symbol)`、
  `stock_profit_forecast_ths(symbol, indicator)`——多半只给
  近端，测了才算数。
- 验收样本：600519 贵州茅台 + **300104 乐视网（已退市）** +
  600074 保千里（已退市）——退市股有数据 = 合格源。

### ② 公募基金全持仓（可能免费，探测后决定买不买）

**要什么**：每只基金每期**全部**持股（股票代码+持股数+占净值
比例），不是十大重仓——中报/年报披露全持仓。

**本地探测**：`akshare` 试 `fund_portfolio_hold_em(symbol=基金
代码, date=年份)`，看返回行数是否 >10——超过即全持仓免费可采，
此项直接从付费清单划掉。

### ③ 新闻/公告文本（备选）

cninfo 公告正文我方已能抓；若本地终端（Wind/东财）有**全市场
快讯/新闻历史导出**（2015+，字段=时间戳+标题+正文+关联股票），
则可开 NLP 轴（届时 Kaggle GPU 才有用武之地）。

### ④ 退市股业绩预告（补缺，若顺手）

东财/同花顺终端导"业绩预告"表看是否含已退市股——含则可替掉
正在跑的 cninfo 补采线。

## B. 落盘格式规范（直接照做，我这边零改动接入）

- 目录：`data/<数据集名>/`，如 `data/consensus/`
- 文件：Parquet 优先（pyarrow 写），CSV 可接受；
  按年/按股分片，**单文件 ≤1.9GB**（GitHub 单资产上限 2GB）
- 每个数据集附 `manifest.json`：{rows, date_min, date_max,
  source, 覆盖缺口说明, 采集时间}
- 股票代码用裸码 `600000` 或 `600000.SH` 均可（我来归一化）

## C. 上传 GitHub Release（与现有 data-20260920b 同规约）

⛔ 不要 `git commit` 数据文件本体——本仓纪律=代码走 git，
数据只走 Release。

**网页法（最简单）**：
1. 打包：`data/<数据集名>/` → `xxx.tar.gz`（或 .zip）
2. GitHub → 仓库 `YZml1507/FinAI2.0` → Releases →
   "Draft a new release"
3. Tag 命名：`data-YYYYMMDD-<字母>`，如 `data-20260922-a`
4. 把压缩包拖进 "Attach binaries" → Publish release
5. 把 tag 名发我

**命令行法（gh CLI）**：
```
gh release create data-20260922-a 文件1.tar.gz 文件2.tar.gz \
  --title "数据包 2026-09-22" --notes "内容说明"
```

我拿到 tag 后接管：下载→解压→manifest 验收→接管线→
冻结预登记→发车。

# 66 — e65 宽篮引擎接入：门禁全通、数据缺口 RCA、择时归因裁决

日期：2026-09-22（深夜连班）
工作项：e65（分数宽篮 → 真引擎路径）+ e66（全 A bars 验收）+ DividendStrategy 潜伏 bug 处置

## 1. 最终结论先行

e65 引擎路径**门禁全通**：17 项后置门 15 PASS + D-4 SKIP（合法）+ E-3 进程内
INCONCLUSIVE（artifact 级已含板价证据，属既有权威口径）。

**择时归因三臂（top100 月调20d 同一分数表、同一窗口 2016-08→2024-12）**：

| 变体 | CAGR | MDD | 换手/年 | 终值(3M) |
|---|---|---|---|---|
| MA200×沪深300（原默认） | 3.26% | 20.3% | 199% | 3.93M |
| MA200×中证1000 | 6.03% | 18.4% | 141% | 4.91M |
| **不择时（终选）** | **12.87%** | **15.7%** | 295% | 8.31M |

裁决：**生产配置取消 MA200 择时**。理由：
① 择时在两指数上分别年耗 9.6pp/6.8pp；
② 不择时臂 MDD 反而最低（15.7% vs 20.3%/18.4%）——宽篮分散自带保护，
择时的卖点（防撞）在本篮不存在：2024 年择时臂破位期仍亏 -15.1%；
③ CSI300 2022 全年破 MA200 → 全年零持仓，错过宽篮实际正收益窗口；
择时规则与小市值风格篮的风险结构不匹配。
④ 成本×3 敏感：费用 236k→708k，终值≈7.84M ⇒ CAGR≈12.1% ≥ 12% 晋级线贴线。

## 2. 数据缺口 RCA（为什么首跑只有 3%）

- baostock 分片 0/3 在 login 后挂死于网络读（3h 仅 100/920，CPU 近零）。
- 2025 段续采先建目录 ⇒ 「目录存在」被误判为已覆盖 ⇒ shard0/3 未竟的
  ~1600 只的 2014-2024 分区整批缺失（sh.600000 只剩 2025/2026）。
- 处置：按「缺任一核心年(2015-2024)」重扫 3065 只幂等合并重拉，0 失败；
  BJ(249 只) baostock 无覆盖且打分集 0 命中 → 显式放弃。
- 最终覆盖：5139 只 ≥3 年分区 + 128 只次新(合法短史) = 全打分域齐。
- 教训：分区级数据校验必须查「分区年表 vs 应有年表」，不能按目录存在判。

## 3. 本段修入清单（均已 commit+push）

1. `scripts/lab/pull_bars_alla.py`：--start/--end（前段已提）。
2. `scripts/run_score_basket_backtest.py`：
   `code_hash=_git_code_hash()`（repro_fingerprint 可生成，REGISTRY 级修复）；
   --no-timing/--index-symbol/--index-path 诊断开关；pit_yields/bar_dates 门上下文。
3. `strategy/candidates.py`：DividendStrategy **两处避险路径困仓每日重试清仓**
   （ma200_timing_avoid + breadth_ice）——e65 同款潜伏 bug 正式处置；
   新回归测试 `test_ma200_avoid_retries_liquidation_daily`。
4. `TEST_BASELINE_PASSED` = 1257（全绿）。
5. 避险语义微调登记：避险解除当日仍重试 SELL（困仓越早出越好），
   两处旧断言按新语义更新。

## 4. 模型 48.5% → 引擎 12.87% 的残差归因（e67 已收单）

向量化两臂分解（e67_fill_attribution.py，89 期 top100 等权持有）：
- 理想臂 close→close：CAGR **48.86%**（与模型 48.5% 逐位对齐，口径正确）
- 现实臂 open→open T+1：CAGR **45.0%**
- ⇒ T+1 开盘漂移成本仅 **~3.9pp/年**（0.21pp/期）——**不是主因**。

真正主因（诊断臂实锤）：**组合层 `min_daily_amount` 默认 5000 万
日成交额过滤**——top100 平均 **46.5%** 被砍（46% 期数过半被砍、
个别期 100%），引擎实际持的是「最流动的一半」，alpha 所在的小票层
被系统性剔除。降到 500 万后同一引擎：

| 臂（均不择时） | CAGR | MDD | 换手/年 | 费用 |
|---|---|---|---|---|
| amt≥5000万（默认） | 12.87% | 15.7% | 295% | 23.6万 |
| **amt≥500万** | **30.65%** | 22.3% | **687.7%** | 106万 |

残差收敛：45.0%（理论现实臂）− 30.65%（引擎）≈ 14pp，余项为
涨停拒/跌停困仓/参与率截断等撮合细节（在鸣量级）。

## 5. 晋级评估（对照冻结口径）

晋升线（e63 prereg）：CAGR≥12% @3x、MDD≤0.35、29 门证据。
- amt≥5000万臂：CAGR 12.87%（@3x≈12.1%）贴线、MDD 0.157（余量足）、
  门：15 PASS + S-2 SKIP（适用性改造后）+ E-3 artifact 级含板价。
- amt≥500万臂：CAGR 30.65%、MDD 0.223——但换手 687.7% 超 S-1 400% 硬顶，
  当前配置**不能晋级**（S-1 FAIL）。
- 裁决路径：压换手至 <400%（reb≥40d）后，预期 CAGR 仍在 20%+ 区间
  → 达标臂进真 registry 跑 29 门做晋级终裁，OOS 2025 观察段随后跟上。

## 6. 下一步

- 换手压制：500 万臂年化换手 687.7% 超 S-1 散户硬顶 400%（¥5 佣金地板
  语义）。h150 信号慢衰 ⇒ 调仓拉长臂（reb=60d，跑中；reb=40d 备选）。
  达标后重跑真 registry 臂→晋级终裁。
- e67 已收单（本文件 §4）；残差余项为撮合细节，不再追。
- OOS：panel_close 已延至 2026-09（bars 齐后重建一次），
  2025 特征+分数（Kaggle GPU e68 系）→ 只观察登记。
- notice_body 正文采集恢复（东财封禁反复，单实例慢速续跑）。
- 2025 daily_basic_alla 无法续（tushare token 缺）→ panel 系列源已切 bars 直建。

[Written by Devin](https://app.devin.ai/sessions/4b3ab4190c73411ca19ca1149a967f75)

# 26 研究面扩张侦察：分钟线可得性 + e26 后数据族盘点

> 2026-09-21 ｜ 性质：**侦察纪要**（非冻结预登记；不产出任何回测结论）
> 方法：① 仓内既有接口打点矩阵 `artifacts/interface_matrix/`（2026-08-30，1081 条接口
> 四态实测）检索；② 本机 akshare 1.18.96 / baostock 0.9.3 现场复测（标注「本次实测」）；
> ③ 文献/规则面核查标注来源或「待核实」。
> 约束假设（散户基线）：万2.5+¥5 保底佣金、印花税卖侧 0.5‰、滑点 5bps、T+1、long-only、
> 日线信号频段。

---

## 一、结论先行

1. **分钟线可得但价值在【执行层】不在【信号层】**。散户费率（单边摩擦 ≥万8 往返）
   下日内信号基本无生存空间——此前判断成立且本次未找到反例证据。但分钟数据对
   本项目有一个合法的低成本用途：**成交时点优化**（当前撮合 = 次日开盘价成交
   `make_price_model`；可测「开盘 vs 尾盘 vs VWAP 时点」的系统性差异，若显著则
   是零换手的纯执行增益）。数据深度是唯一硬伤：免费渠道 1m 仅近期、5m 最深
   约 2020 年起（本次实测），做「分钟级执行回放」对 2015–2019 段无解 →
   **建议：执行优化假设只允许在数据可得窗（~2021+）内做影子估计，不进冻结臂**。
2. **下一波数据族性价比排序**（依据：仓内接口矩阵四态 + 本次实测 + 文献证据强度）：
   | 序 | 数据族 | 接口状态 | 散户证据 | 决策 |
   |---|--------|---------|---------|------|
   | e27 | **高管/内部人增减持** | 巨潮 `stock_hold_management_detail_cninfo`（矩阵 OK 10039 行）+ 雪球 `stock_inner_trade_xq`（OK 24532 行） | A 股内部人买入对后续收益有稳健正向证据 | **首推发车** |
   | e28 | **股东人数/筹码集中度** | `stock_zh_a_gdhs_detail_em`（本次实测 OK：600519 有 63 期 2013→2026）；`stock_hold_num_cninfo`（OK 4133 行）；efinance `get_latest_holder_number`（OK 5542） | 散户流出=筹码集中，中文文献/研报证据中等 | 第二顺位 |
   | e29 | **龙虎榜 + 主力资金流** | 龙虎榜 19 接口全 OK；资金流 `stock_main_fund_flow`/`stock_individual_fund_flow` OK | 数据脏但可做事件研究；**附带解锁 M5 重启条件②（行为数据交叉验证）** | 第三顺位（兼有复核价值） |
   | e30 | **回购/解禁事件** | `stock_repurchase_em` OK 5516（本次实测 53.6s）；`stock_restricted_release_detail_em` OK 5522 | 回购公告正向、解禁负向，均为稀疏事件族 | 合并为「公司行动事件族」一臂 |
   | — | 北向持股日频 | `stock_hsgt_hold_stock_em` 矩阵 FAIL_GATEWAY + 本次实测 TypeError：**2024-08-19 起北向持股明细改为季度披露**（规则变更，待附引用） | — | **关闭**（日频轴已不存在） |
   | — | 股权质押 / 机构调研 / 评级一致预期 / 舆情热度 | 矩阵均 OK（质押比例 2294、jgdy 194、institute_recommend 5126、hot_rank_em） | 证据弱/口径脏 | 入候选池，暂不立项 |
   | — | 行业分类历史版 | `stock_industry_clf_hist_sw` OK 12891 行（申万历史分类！） | 使能数据非信号 | **优先采集**（打开行业中性化/轮动层，所有后续族受益） |
   | — | 可转债 | `bond_zh_cov` OK 1047（本次实测） | 规则多次变更（2022-08 起转债也 T+0→有涨跌限） | 独立标的面，若立项需新管道，暂缓 |

3. **M5（融券余量）重启条件②已被本盘点证实可满足**：资金流/龙虎榜接口在矩阵中
   全绿，采集后即可做交叉验证复核——不需要新外部依赖。

## 二、分钟线实测明细（2026-09-21 本机）

| 渠道 | 接口 | 实测结果 | 深度 |
|------|------|---------|------|
| 新浪 | `ak.stock_zh_a_minute(600519, 1m)` | OK 1970 行 | 仅近期（数日级） |
| 东财 | `ak.stock_zh_a_hist_min_em(600519, 5m/1m)` | ConnectionError（矩阵 2026-08-30 记 OK 1488 行——本机网络抖动/东财限流，不可解释为不可用） | 1m 仅近期（仓内注释：只留近期）；5m 较长 |
| baostock | `query_history_k_data_plus` | 5m：2021-01+ OK（144 行/日=正确格数）；2019-12 及更早 0 行；1m 报错 10004012（不支持） | **≈2020 年起**，无 1m |
| 腾讯 | `stock_zh_a_tick_tx_js` | 矩阵 OK 4565 行 | 历史分笔，深度待验 |
| TDX | mootdx/tdxpy minute 族 | 矩阵 OK（330/240/3408 行） | 商用终端通道，覆盖与稳定性需专测 |

**含义**：任何依赖分钟级执行回放的研究，有效样本窗天然被压在 ~2021-2026 这 5.7 年，
与主回测窗 2015-2024 不重合 → 分钟级证据只能作 OOS 旁证，不能进主预登记的冻结窗。

## 三、数据族卡片（关键字段）

### e27 内部人增减持（首推）
- 接口：`stock_hold_management_detail_cninfo`（董监高持股变动明细，矩阵 10039 行）、
  `stock_inner_trade_xq`（雪球内部交易 24532 行）、`stock_hold_control_cninfo`（实控人 5568）、
  `stock_share_hold_change_sse/szse`（本次实测 KeyError——接口字段漂移，备选巨潮/雪球）。
- 假设族草样：董监高净买入强度 20d/60d、增持金额/流通市值、高管增持公告事件窗（+1/+5/+20 超额）、
  实控人减持折价砸盘（反向）。方向先验：多净增持（内部人信息优势）。
- PIT 难点：以**公告日**为可见时点，不是变动日——巨潮字段含公告日，须锁定用公告日 PIT。
- 风险：小票自买自拉/市值管理噪音；样本稀疏（月频级事件）； survivors ok（巨潮含退市票待验）。

### e28 股东人数（次推）
- 接口：`stock_zh_a_gdhs_detail_em`（个股历史期序列，本次实测 600519 = 63 期 2013-03→2026-06，
  季频为主+部分月度）；`stock_zh_a_gdhs`（按披露期全市场快照，本次实测 2025Q2 = 5347 行）；
  `stock_hold_num_cninfo`（巨潮股东人数及集中度 4133）；`efinance get_latest_holder_number`。
- 假设族草样：股东户数 1 期环比（多减少=筹码集中）、户数变化率 z-score、户均持股市值变化。
  方向先验：多户数下降。
- PIT 难点：**披露滞后天然 PIT 安全**（统计截止日 ≠ 可见日，须按公告披露日落地——akshare
  行内含截止日，可见日需另建披露日历）。
- 风险：披露频率不均（季报必披、月度自愿）、早期覆盖稀薄。

### e29 龙虎榜/资金流（第三，兼 M5 复核件）
- 龙虎榜：19 接口矩阵全 OK（`lhb_detail_em` 本次实测 645 行/10 日、`lhb_stock_detail_em`、
  `lhb_yybph_em` 营业部榜、`lhb_jgmmtj` 机构买卖统计）。数据脏（上榜原因杂、席位命名漂移）。
- 资金流：`stock_main_fund_flow`（主力净流入排名 5292）、`stock_individual_fund_flow`、
  `stock_fund_flow_individual`（同花顺 5200）。⚠️ 「主力净流入」为成交额分档规则合成，
  方法论不透明——只能作交叉验证材料，不宜单独立假设。
- 价值定位：不只假设族，更是 **M5 融券信号的行为侧复核数据源**（重启条件②）。

### 使能数据：申万行业分类历史版
- `stock_industry_clf_hist_sw` 矩阵 OK 12891 行——申万历史分类全序列。
- 采集后解锁：行业中性化、行业轮动、同业相对强弱——**所有后续族的基础设施工件**，
  采集成本最低（单表），建议先于任何假设族落盘。

### 关闭/暂缓
- **北向日频持股**：2024-08-19 起披露降级为季度（接口矩阵 FAIL_GATEWAY 与本次实测一致）→ 关闭。
- **可转债**：标的层面不同（T+0→限涨、无印花税、发行人不同），需独立管道 → 暂缓。
- 股权质押比例、机构调研、分析师评级/盈利预测（`stock_profit_forecast_em` 2814 行）、
  雪球/东财热度：进候选池登记，证据强度不足以单独立项，可作未来合成臂素材。

## 四、建议执行序列（不做即不冻结）

1. **采集先行（无预登记负担）**：申万行业历史分类 → 内部人增减持明细 → 股东人数 →
   龙虎榜/资金流（兼 M5 复核）。每源沿用 `scripts/lab/collect_*` 模式：幂等断点、
   fails>0 告警、落盘 data/<family>/（gitignored，与 margin_detail 同制）。
2. e27 预登记草案：内部人净增持族 4-6 假设，沿用 e25/e26 双闸骨架（Bonferroni、
   G4 覆盖闸、方向先验冻结）。
3. 执行优化（分钟层）单独登记为「工程改进实验」不进 alpha 假设族：
   开盘/尾盘/VWAP 时点价差影子估计，仅 2021+ 窗。

## 五、风险与未决

- 东财端点对本机间歇性 ConnectionError（本次 hist_min_em、board_industry_name_em 失败，
  但矩阵中同接口 OK）——网络层抖动非接口失效；采集器需带 host 池重试（仓内已有
  `install_eastmoney_pool_retry` 垫片）。
- 本次实测未逐接口二分全部深度（股东人数/增减持的最早覆盖年待建库时验证）。
- 文献证据栏引用的「A 股有效性」均为二手综述级判断，发车预登记前须按族补一手引用。
